/** @type {import('next').NextConfig} */
const nextConfig = {
  //   distDir: "build",
  output: "export",
  //   assetPrefix: "./",
};

const withFonts = require("next-fonts");

module.exports = nextConfig;
